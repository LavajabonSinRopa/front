No modularice la logica para ordenar los elementos. El que se agrega va al ultimo. Colta.
Hay dos opcciones de websocket en App.jsx, uno para "npm run dev" y otro para "npm test"

para ejecutar el tests:
npm i
npm install ws
(capas faltan mas dependencias que no recuerdo ahora, dale el error que te salga a gepeto y el te dice)
npm test
